var first_number = prompt("Dammi un numero");

var second_number = prompt("Another one");

if (first_number > second_number) {
    console.log(first_number);
  } else {
    console.log(second_number);
  }
